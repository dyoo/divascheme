(module info (lib "infotab.ss" "setup")
  (define name "struct")
  (define blurb
    (list "A library of various data structures based on structs, "
          "including algebraic datatypes and inheritance hierarchies."))
  (define primary-file "datatype.ss")
  (define doc.txt "doc.txt")
  (define categories '(datastructures))
  (define version "3"))
